import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function(){
    return(
        <SafeAreaView>
        <View>
        <Text> hello from author</Text>
        </View>
        </SafeAreaView>
    )
}
